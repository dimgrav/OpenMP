#include <stdio.h>

int main(int argc, char** argv) {
int P;
int myid;
# pragma omp parallel  
{
	myid = omp_get_thread_num();
	printf("Hello world apo tin diergasia %d\n", myid);
	if (myid == 0) {
		P = omp_get_num_threads();
		printf("synolo diergasion = %d\n", P);
	}
}
return 0;
}
